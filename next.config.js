/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["i.dummyjson.com", "dummyjson.com", "universal-tutorial.com"],
  },
};

module.exports = nextConfig;
